name : Test&Analyze
on :
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  linting:
    runs-on: ubuntu latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Kotlin Linter
        uses: vroy/gha-kotlin-linter@v4
